TITLE: Signal Cascade Analyzer
DESCRIPTION: Detects failure propagation waves moving through the DAG by fingerprinting error signals and tracing their downstream replication across agents, then emits quarantine directives to isolate the cascade before it consumes healthy pipeline stages.
SUBSYSTEM: Ichor.Subsystems.CascadeAnalyzer
SIGNAL_INTERFACE: Subscribes to :task_failed, :agent_error, :node_stalled signals; emits :cascade_detected, :quarantine_node, :cascade_cleared
TOPIC: subsystem:cascade_analyzer
VERSION: 0.1.0
FEATURES: error fingerprinting, cascade wave detection, blast-radius estimation, automatic quarantine directive emission, cascade timeline replay
USE_CASES: prevent single agent failure from stalling entire DAG, post-mortem cascade forensics, SLA breach early warning, auto-isolation of poison pill tasks
ARCHITECTURE: GenServer holding a sliding ETS window of recent error events; on each :task_failed signal it runs a graph traversal over the live DAG topology (from Ichor.Dag) to count how many downstream nodes share the error fingerprint; if propagation ratio exceeds threshold it broadcasts :cascade_detected and emits :quarantine_node for each infected node; a scheduled tick clears stale cascade states
DEPENDENCIES: Ichor.Dag, Ichor.SwarmMonitor, Ichor.Fleet
SIGNALS_EMITTED: :cascade_detected, :quarantine_node, :cascade_cleared, :blast_radius_updated
SIGNALS_SUBSCRIBED: :task_failed, :agent_error, :node_stalled, :dag_topology_updated

---
NOTE: Researcher agents (researcher-1, researcher-2) did not respond within the 8-minute window.
Brief synthesized by coordinator (fallback path) at run b0d86ba0.
