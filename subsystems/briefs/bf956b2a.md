TITLE: Adaptive Signal Entropy Monitor
DESCRIPTION: Monitors signal entropy across agent fleets to detect anomalous behavior patterns and trigger corrective actions. Implements Shannon entropy scoring over sliding windows of signal streams, flagging deviations that indicate fleet degradation or coordination failures.
SUBSYSTEM: Ichor.Subsystems.EntropyMonitor
SIGNAL_INTERFACE: :agent_heartbeat, :agent_signal, :fleet_state_change
TOPIC: subsystem:entropy_monitor
VERSION: 0.1.0
FEATURES: sliding-window entropy scoring, per-agent entropy tracking, fleet-level aggregate entropy, anomaly threshold alerting, self-healing trigger emission
USE_CASES: detect agent silence (high entropy), detect coordinated failure (entropy spike), validate fleet health before run dispatch, trigger escalation on sustained entropy anomaly
ARCHITECTURE: GenServer holding per-agent entropy state maps; PubSub subscriber consuming :agent_heartbeat and :agent_signal topics; sliding window (configurable N=50 events) computes Shannon entropy H = -sum(p*log2(p)) per agent; fleet aggregate = mean of agent entropies; threshold breach emits :entropy_alert signal; ETS cache for O(1) per-agent lookup
DEPENDENCIES: Ichor.Registry, Ichor.Signals.Catalog, Phoenix.PubSub, Ichor.Fleet.AgentProcess
SIGNALS_EMITTED: :entropy_alert, :entropy_nominal, :fleet_entropy_report
SIGNALS_SUBSCRIBED: :agent_heartbeat, :agent_signal, :fleet_state_change, :agent_terminated

---
MES run: bf956b2a
Coordinator note: Researchers dispatched but no responses received within deadline window.
Brief synthesized by coordinator from domain knowledge covering entropy management (researcher-1 domain) and anomaly detection (researcher-2 domain).
