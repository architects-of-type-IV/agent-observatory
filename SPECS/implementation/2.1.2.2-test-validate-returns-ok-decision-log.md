---
id: "2.1.2.b"
title: "Test validate/1 returns {:ok, %DecisionLog{}} for fully valid params"
status: pending
task_id: "2.1.2"
governed_by: ["ADR-015"]
parent_uc: ["UC-0210"]
---

goal: "Add a test tagged @tag :validate that builds a valid string-keyed params map with all required DecisionLog fields, calls SchemaInterceptor.validate(params), and asserts the return matches {:ok, %Observatory.Mesh.DecisionLog{}}"
allowed_files:
  - test/observatory/gateway/schema_interceptor_test.exs
blocked_by: ["2.1.2.a"]
steps:
  - In schema_interceptor_test.exs, add a test tagged @tag :validate
  - Build a valid string-keyed params map containing all required fields
  - Call SchemaInterceptor.validate(params)
  - Assert the return value matches {:ok, %Observatory.Mesh.DecisionLog{}}
done_when: "mix test test/observatory/gateway/schema_interceptor_test.exs --only validate"
