# Task 2.4.1.1: message_composer/1 component

Goal: Multi-target message sending UI
Files: lib/observatory_web/components/team_message_components.ex
Steps:
  1. Create module ObservatoryWeb.Components.TeamMessageComponents
  2. use Phoenix.Component
  3. Attrs: teams (list), selected_message_target (any, default nil)
  4. Render:
     - Container bg-zinc-900 border border-zinc-800 rounded-lg p-3
     - Target selector (select dropdown with phx-change="set_message_target")
     - Options: "All teams", per-team options, "All leads", per-team lead, per-team members, "All members", per-agent
     - Message input + send button (phx-submit="send_targeted_message")
     - Target count display
Done when: `mix compile --warnings-as-errors` passes
