# Task 2.4.3.2: Wire into template

Goal: Add teams tab, view dispatch, inspector drawer, tmux view
Files: lib/observatory_web/live/dashboard_live.html.heex
Steps:
  1. Add "Teams" button to #view-mode-toggle (after Timeline)
  2. Add TeamsComponents.teams_view :if={@view_mode == :teams} with attrs
  3. Add TeamMessageComponents.message_composer inside teams view
  4. Add TeamInspectorComponents.inspector_drawer OUTSIDE view :if blocks (always rendered)
  5. Add TeamTmuxComponents.tmux_view :if={@inspector_maximized} (overlay)
Done when: template renders
