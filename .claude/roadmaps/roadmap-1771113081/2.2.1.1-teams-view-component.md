# Task 2.2.1.1: teams_view/1 component

Goal: Main teams view with team card rows
Files: lib/observatory_web/components/teams_components.ex
Steps:
  1. Create module ObservatoryWeb.Components.TeamsComponents
  2. use Phoenix.Component
  3. Import ObservatoryWeb.Components.ObservatoryComponents, DashboardFormatHelpers
  4. Attrs: teams (list, required), inspected_teams (list, default []), now (any, required)
  5. Render:
     - Section header "Teams" with count badge
     - Grid of team cards (bg-zinc-900/50, border-zinc-800)
     - Each card: team name, member count with status dots, health dot, task progress "N/M", "Inspect" button
     - Inspected teams get border-indigo-500/40 highlight
     - Empty state via ObservatoryComponents.empty_state
  6. Add private team_row/1 component for each card
Done when: `mix compile --warnings-as-errors` passes
