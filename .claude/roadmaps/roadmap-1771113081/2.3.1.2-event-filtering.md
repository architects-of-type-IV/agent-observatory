# Task 2.3.1.2: Event filtering by output mode

Goal: Filter events based on selected output mode
Files: lib/observatory_web/components/team_tmux_components.ex
Steps:
  1. Add filter_events_for_mode/3 (events, mode, team)
  2. Modes: :all_live, :leads_only, :lead_to_members, :members_to_lead, :member_to_member, :all_agents
  3. Communication modes filter on SendMessage tool events, parse payload
Done when: filtering correct for all 6 modes
