# Story 2.1.2: Multi-Target Messaging Functions

## Delivers
Backend functions for sending messages to various target scopes.

## Acceptance Criteria
- `broadcast_to_many/4` sends to a list of session_ids via Mailbox
- `resolve_message_targets/2` resolves target scope to session_id list
- `mix compile --warnings-as-errors` passes

## Files
- `lib/observatory/mailbox.ex` (modify)
- `lib/observatory_web/live/dashboard_team_helpers.ex` (modify)
