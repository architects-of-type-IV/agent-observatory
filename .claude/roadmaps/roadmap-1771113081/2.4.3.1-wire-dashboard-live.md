# Task 2.4.3.1: Wire into dashboard_live.ex

Goal: Add assigns, imports, handle_event clauses, atom safety, stale team pruning
Files: lib/observatory_web/live/dashboard_live.ex
Steps:
  1. Import DashboardTeamInspectorHandlers
  2. Alias 4 new component modules: TeamsComponents, TeamInspectorComponents, TeamTmuxComponents, TeamMessageComponents
  3. Add @view_modes module attribute listing all 10 view modes (forces :teams atom to exist at compile time for String.to_existing_atom)
  4. In mount, add assigns: inspected_teams: [], inspector_layout: :horizontal, inspector_maximized: false, inspector_size: :default, output_mode: :all_live, agent_toggles: %{}, selected_message_target: nil
  5. Add handle_event clauses using guard group pattern: `when e in ["inspect_team", "remove_from_inspector", ...]`
  6. In handle_info({:teams_updated, ...}), filter inspected_teams to only include teams that still exist
Done when: `mix compile --warnings-as-errors` passes
