# Task 2.1.1.1: detect_role/2

Goal: Determine if a team member is lead or member
Files: lib/observatory_web/live/dashboard_team_helpers.ex
Steps:
  1. Read the existing module
  2. Add `detect_role(team, member)` that checks if member matches team.lead_session or is first member in config
  3. Return :lead or :member atom
Done when: `mix compile --warnings-as-errors` passes
