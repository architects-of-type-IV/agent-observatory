# Story 2.2.2: Inspector Drawer

## Delivers
Bottom slide-up drawer holding inspected team panels in a stack. Layout toggle (horizontal/vertical), maximize button, resize handle.

## Acceptance Criteria
- Drawer at bottom of viewport when teams inspected
- Empty: thin collapsed bar "Click a team to inspect"
- Populated: team panels in stack
- Add/remove teams via inspect_team/remove_from_inspector
- Layout toggle: horizontal (side-by-side) vs vertical (stacked)
- Maximize button expands to fill viewport
- Resize handle on top edge
- Smooth CSS transition
- Module under 300 lines
- `mix compile --warnings-as-errors` passes

## Files (EXCLUSIVE to Agent B)
- `lib/observatory_web/components/team_inspector_components.ex` (create)
- `assets/js/app.js` (modify -- add InspectorDrawer hook)
- `assets/css/app.css` (modify -- add drawer CSS)
