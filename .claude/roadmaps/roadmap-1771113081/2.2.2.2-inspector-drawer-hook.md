# Task 2.2.2.2: InspectorDrawer JS hook

Goal: Handle drawer resize via drag and height persistence
Files: assets/js/app.js
Steps:
  1. Add InspectorDrawer hook to hooks object
  2. mounted(): set up mousedown on resize handle, mousemove for height, mouseup to stop
  3. Push "resize_inspector" event with new height
  4. Persist height to localStorage
Done when: hook registers without JS errors
