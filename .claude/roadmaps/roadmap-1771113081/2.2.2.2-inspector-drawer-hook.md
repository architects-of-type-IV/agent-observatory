# Task 2.2.2.2: Inspector drawer hooks (v1: state toggle + auto-scroll)

Goal: Drawer state toggle and auto-scroll for tmux panes
Files: assets/js/app.js
Steps:
  1. Add InspectorDrawer hook to Hooks object (lines 28-253)
  2. mounted(): persist drawer state (collapsed/default/maximized) to localStorage
  3. handleEvent("set_drawer_state"): update localStorage, no drag resize in v1
  4. Add AutoScrollPane hook (same pattern as AutoScrollTimeline from BRAIN.md)
  5. mounted(): scrollToBottom(), updated(): scroll if near bottom (<100px)
  6. v2 deferred: drag resize with mousedown/mousemove/mouseup
Done when: hooks register without JS errors
