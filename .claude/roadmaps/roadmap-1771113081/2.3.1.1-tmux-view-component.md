# Task 2.3.1.1: tmux_view/1 component

Goal: The maximized tiled layout with panes
Files: lib/observatory_web/components/team_tmux_components.ex
Steps:
  1. Create module ObservatoryWeb.Components.TeamTmuxComponents
  2. use Phoenix.Component, import format helpers
  3. Attrs: inspected_teams (list), output_mode (atom), agent_toggles (map), live_events (list), now (any)
  4. Render:
     - Top bar: output mode buttons (6 modes), close maximize button
     - CSS grid adapting to team count
     - Each pane: header (team name, member checkboxes), scrollable event stream
Done when: `mix compile --warnings-as-errors` passes
