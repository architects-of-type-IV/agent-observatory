# Task 2.1.2.1: broadcast_to_many/4

Goal: Generic multi-target send via Mailbox
Files: lib/observatory/mailbox.ex
Steps:
  1. Add `broadcast_to_many(session_ids, from, content, opts \\ [])`
  2. Iterates and calls send_message per session_id
  3. Returns list of {:ok, msg} | {:error, reason}
Done when: `mix compile --warnings-as-errors` passes
