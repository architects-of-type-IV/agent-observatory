# Task 2.2.2.1: inspector_drawer/1 component

Goal: The bottom drawer container with team panels
Files: lib/observatory_web/components/team_inspector_components.ex
Steps:
  1. Create module ObservatoryWeb.Components.TeamInspectorComponents
  2. use Phoenix.Component
  3. Attrs: inspected_teams (list), inspector_layout (:horizontal | :vertical), inspector_maximized (boolean), inspector_height (integer, default 300)
  4. Render:
     - Fixed bottom, bg-zinc-900, border-t border-zinc-800
     - Empty: h-8 bar with "Click a team to inspect" text-zinc-600
     - Populated: header bar (Inspector label, count, layout toggle, maximize, close-all) + content flex container
     - Each team panel: name header, remove X button, member list, status
     - Maximized: fixed inset-0 z-40
  5. phx-hook="InspectorDrawer" for resize
  6. transition-all duration-200 ease-in-out
Done when: `mix compile --warnings-as-errors` passes
